{% set messageData = request.session.pop_flash() %}
{% if messageData|length > 0 %}
    messageData = '{{ messageData[0] }}';
    swal({
        title: 'FormShare',
        text: messageData,
        type: "success"
    });
{% endif %}