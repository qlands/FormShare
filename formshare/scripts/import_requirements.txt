# Requirements for the FormShare ElasticSearch IMPORT script
#   formshare/scripts/importelasticsearch.py
#
# Install this in the environment that connects to the NEW ElasticSearch
# 9.2.x server (the FormShare 3.x stack). 9.2.0 matches the pin in FormShare's
# master-3.0 branch and is compatible with the 9.2.1 server.
#
# Use a dedicated virtualenv so it does not clash with the 7.x client:
#
#   python3 -m venv venv-es9
#   ./venv-es9/bin/pip install -r formshare/scripts/import_requirements.txt
#   ./venv-es9/bin/python formshare/scripts/importelasticsearch.py \
#       --ini /path/to/new.ini --input ./es_dump
#
elasticsearch==9.2.0
