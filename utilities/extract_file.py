from formshare.processes.storage import get_stream

# project = ""
#
# bucket_id = project + form
# bucket_id = md5(bucket_id.encode("utf-8")).hexdigest()
#     stream = get_stream(request, bucket_id, file_name)
